using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using DG.Tweening;
public class pathcscript : MonoBehaviour
{
    public DOTweenPath dp;

    private void Awake()
    {
       
    }
    // Start is called before the first frame update
    void Start()
    {
        dp.DOPause();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    public void dpdpdp()
    {
       // dp.DOKill();
    }
}
